INCLUDE_DIRECTORIES (${OHCS_SOURCE_DIR}/include)
INCLUDE_DIRECTORIES (${OHCS_SOURCE_DIR}/lib)
LINK_DIRECTORIES (${OHCS_BINARY_DIR}/lib)
ADD_EXECUTABLE (
	ohcs
	argv.cpp
	main.cpp
)
TARGET_LINK_LIBRARIES (ohcs del bigint OCSS)
